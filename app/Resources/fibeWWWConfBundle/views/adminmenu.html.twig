<div class="navbar navbar-static-top navbar-inverse">
    <div class="navbar-inner">
        <div class="container">
            <a class="btn btn-navbar" data-toggle="collapse" data-target=".navbar-responsive-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
				<span class="glyphicon glyphicon-resize-small glyphicon-white ">
            </a>
            <a class="brand" href="{{ path('wwwconf_link_index') }}" title="Go back to Index">WWWConf</a>
              <div class="nav-collapse collapse navbar-responsive-collapse">
                <ul class="nav">
                    <li>
                        <a href="{{ path('wwwconf_schedule') }}" title="manage schedule">Schedule</a>
                    </li>
                    
                    
                    {% if app.user.wwwConf %}
                    <li class="dropdown">
                        <a href="{{ path('wwwconf_schedule_confId', { 'confId': app.user.wwwConf.id}) }}" class="dropdown-toggle" data-toggle="dropdown">
                            <b class="caret"></b>
                        </a> 
                    </li> 
                    {% endif %}
                    
                    
                    <li class="divider-vertical"></li>
                    <li class="divider-vertical"></li>
                    <li>
                        <a href="{{ path('admin_schedule_status') }}" title="Status">Status</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_schedule_location') }}" title="Location">Location</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_schedule_category') }}" title="Category">Category</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_schedule_event') }}" title="Event">Event</a>
                    </li>
					 <li>
                        <a href="{{ path('wwwconf_person_index') }}" title="Person">Person</a>
                    </li>
                </ul>
                <ul class="nav pull-right">
                  <li>
                        <a href="{{ path('logout') }}" title="logout" class='btn btn-danger'><span class="glyphicon glyphicon-off glyphicon-white "></span></a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</div>
