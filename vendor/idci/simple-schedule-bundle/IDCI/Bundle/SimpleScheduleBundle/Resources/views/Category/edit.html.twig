{% extends 'fibeWWWConfBundle::base.html.twig' %}

{% block body %}



<div class="page-header">
  <h1>{% trans %}Edit{% endtrans %}</h1>
</div>
 
<form action="{{ path('schedule_category_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit" class="btn btn-inverse"><span class="icon-ok  glyphicon-white"></span></button>
    </p>
</form>
<ul class="record_actions">
    <li>
        <a class="btn btn-warning" href="{{ path('schedule_category') }}">
            <span class="icon-list  glyphicon-white"></span>
        </a>
    </li>
    <li>
        <form action="{{ path('schedule_category_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit" class="btn btn-danger"><span class="icon-remove  glyphicon-white"></span></button>
        </form>
    </li>
</ul>
{% endblock body %}
