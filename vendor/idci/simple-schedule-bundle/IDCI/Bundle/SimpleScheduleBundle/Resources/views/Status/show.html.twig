{% extends "IDCISimpleScheduleBundle::adminbase.html.twig" %}

{% block body %}

{{ parent() }}

<div class="page-header">
  <h1>Status</h1>
</div>

<table class="table table-hover record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Value</th>
            <td>{{ entity.value }}</td>
        </tr>
    </tbody>
</table>


        <a class="btn btn-success" href="{{ path('admin_schedule_status') }}">
           <span class="icon-list glyphicon-white"></span>
        </a>

        <a class="btn btn-warning" href="{{ path('admin_schedule_status_edit', { 'id': entity.id }) }}">
            <span class="icon-edit glyphicon-white"></span>
        </a>

{% endblock body %}
