{% extends 'fibeWWWConfBundle::base.html.twig' %}

{% block body %}

{{ parent() }}


<div class="page-header">
    <h1>{% trans %}Locations{% endtrans %}</h1>
</div>

<p>
    {{ pagerfanta(pager, 'twitter_bootstrap') }}
</p>

<ul class="list_actions">
    <li>
        <a class="btn btn-info" href="{{ path('schedule_location_new') }}">
            <span class="icon-plus"></span>
        </a>
    </li>
</ul>

</br>
<table class="table table-hover records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Description</th>
            <th>Latitude</th>
            <th>Longitude</th>
            <th colspan="3">{% trans %}Actions{% endtrans %}</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in pager.currentPageResults %}
        <tr>
            <td><a href="{{ path('schedule_location_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.name }}</td>
            <td>{{ entity.description }}</td>
            <td>{{ entity.latitude }}</td>
            <td>{{ entity.longitude }}</td>
            <td>
                <a class="btn btn-block btn-success btn-small" href="{{ path('schedule_location_show', { 'id': entity.id }) }}"><span class="icon-eye-open  glyphicon-white"></span></a>
            </td>
            <td>
                <a class="btn btn-block btn-warning btn-small" href="{{ path('schedule_location_edit', { 'id': entity.id }) }}"><span class="icon-edit  glyphicon-white"></span></a>
             </td> 
        </tr>
    {% endfor %}
    </tbody>
</table>




{% endblock body %}
