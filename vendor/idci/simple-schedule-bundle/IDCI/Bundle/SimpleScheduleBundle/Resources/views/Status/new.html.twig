{% extends 'fibeWWWConfBundle::base.html.twig' %}

{% block body %}

{{ parent() }}


<div class="page-header">
  <h1>{% trans %}Status creation{% endtrans %}</h1>
</div>
<div class="row">
  <div class="col-lg-2"></div>
  <div class="col-lg-8">
    <form action="{{ path('schedule_status_create', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
   </div>
   <div class="row-fluid">
        <div class="col-lg-5"></div>
        <div class="col-lg-2">
            </br><button type="submit" class="btn btn-default"><i class="icon-ok"></i></button> 
            <a class="btn btn-warning" href="{{ path('schedule_status') }}">
            	<i class="icon-list"></i>
        	</a>   
          </div>
    </form>
    </div>
{% endblock body %}
