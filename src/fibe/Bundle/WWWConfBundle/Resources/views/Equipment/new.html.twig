{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Equipment creation</h1>

    <form action="{{ path('equipment_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit" class="btn btn-default"><span class="glyphicon glyphicon-ok"></span></button>
        </p>
    </form>

        <ul class="record_actions">
   
        <a href="{{ path('equipment') }}">
             <a  class="btn btn-warning" href="{{ path('equipment_new') }}">
                 <span class="glyphicon glyphicon-list glyphicon-white"></span>
            </a>
        </a>
 
{% endblock %}
