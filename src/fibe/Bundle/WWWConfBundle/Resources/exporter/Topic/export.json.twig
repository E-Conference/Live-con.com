{
    "id"          : "{{ entity.id }}",
    "name"       : "{% autoescape 'js' %}{{ entity.name }}{% endautoescape %}",
	"papers":
	[
	{% for paper in entity.papers %}
	    {% if loop.first %}
	    { "id": "{{ paper.id }}", "name": "{% autoescape 'js' %}{{ paper.title }}{% endautoescape %}"}
	    {% else %}
	    ,{ "id": "{{ paper.id }}", "name": "{% autoescape 'js' %}{{ paper.title }}{% endautoescape %}"}
	    {% endif %}
	{% endfor %}
	],
	"events":
	[
	{% for event in entity.events %}
	    {% if loop.first %}
	    { "id": "{{ event.id }}", "name": "{% autoescape 'js' %}{{ event.name }}{% endautoescape %}"}
	    {% else %}
	    ,{ "id": "{{ event.id }}", "name": "{% autoescape 'js' %}{{ event.name }}{% endautoescape %}"}
	    {% endif %}
	{% endfor %}
	]
}