{
    "id"          : "{{ entity.id }}",
    "name"       : {% if entity.name != null %}{{entity.name|json_encode|raw()}}{% else %}""{% endif %},
	"papers":
	[
	{% for paper in entity.papers %}
	    {% if loop.first %}
	    { "id": "{{ paper.id }}", "name": {% if paper.title != null %}{{paper.title|json_encode|raw()}}{% else %}""{% endif %}}
	    {% else %}
	    ,{ "id": "{{ paper.id }}", "name": {% if paper.title != null %}{{paper.title|json_encode|raw()}}{% else %}""{% endif %}}
	    {% endif %}
	{% endfor %}
	],
	"events":
	[
	{% for event in entity.events %}
	    {% if loop.first %}
	    { "id": "{{ event.id }}", "name": {% if event.summary != null %}{{event.summary|json_encode|raw()}}{% else %}""{% endif %}}
	    {% else %}
	    ,{ "id": "{{ event.id }}", "name": {% if event.summary != null %}{{event.summary|json_encode|raw()}}{% else %}""{% endif %}}
	    {% endif %}
	{% endfor %}
	]
}