{
    "id": "{{ entity.id }}",
    "name": {{ entity.name|json_encode|raw()  }},
    "description": {{ entity.description|json_encode|raw()  }},
    "latitude": "{{ entity.latitude }}",
    "longitude": "{{ entity.longitude }}"
}
