{
    "id"       : "{{ entity.id }}",
    "name"     : "{% autoescape 'js' %}{{ entity.name }}{% endautoescape %}",
    "page"     : "{% autoescape 'js' %}{{ entity.page }}{% endautoescape %}",
    "country"  :  "{{ entity.country }}",
    "members":
            [
            {% for member in entity.bouze %}
                {% if loop.first %}
                { "id": "{{ member.id }}", "name": "{{ member.name() }}"}
                {% else %}
                ,{ "id": "{{ member.id }}", "name": "{{ member.name() }}"}
                {% endif %}
            {% endfor %}
            ] 

} 