{
     "id": "{{ entity.id }}",
     "title": "{% autoescape 'js' %}{{ entity.title }}{% endautoescape %}",
     "abstract": "{% autoescape 'js' %}{{ entity.abstract }}{% endautoescape %}",
     "publishDate": "{{ entity.publishDate }}" , 
     "url": "{% autoescape 'js' %}{{ entity.url }}{% endautoescape %}" ,       
     "publisher": "{{ entity.publisher }}" , 
     "topics":
            [
            {% for topic in entity.topics %}
                {% if loop.first %}
                { "id": "{{ topic.id }}", "name": "{% autoescape 'js' %}{{ topic.name }}{% endautoescape %}"}
                {% else %}
                ,{ "id": "{{ topic.id }}", "name": "{% autoescape 'js' %}{{ topic.name }}{% endautoescape %}"}
                {% endif %}
            {% endfor %}
            ],
     "authors":
            [
            {% for author in entity.authors %}
                {% if loop.first %}
                { "id": "{{ author.id }}", "name": "{% autoescape 'js' %}{{ author.name }}{% endautoescape %}"}
                {% else %}
                ,{ "id": "{{ author.id }}", "name": "{% autoescape 'js' %}{{ author.name }}{% endautoescape %}"}
                {% endif %}
            {% endfor %}
            ]
         
}
