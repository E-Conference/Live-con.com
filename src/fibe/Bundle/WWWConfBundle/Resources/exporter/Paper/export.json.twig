{
     "id": "{{ entity.id }}",
     "title": {% if entity.title != null %}{{entity.title|json_encode|raw()}}{% else %}""{% endif %},
     "abstract": {% if entity.abstract != null %}{{entity.abstract|json_encode|raw()}}{% else %}""{% endif %},
     "publishDate": "{{ entity.publishDate }}" , 
     "url": {% if entity.url != null %}{{entity.url|json_encode|raw()}}{% else %}""{% endif %} ,       
     "publisher": "{{ entity.publisher }}" , 
     "topics":
            [
            {% for topic in entity.topics %}
                {% if loop.first %}
                { "id": "{{ topic.id }}", "name": {% if topic.name != null %}{{topic.name|json_encode|raw()}}{% else %}""{% endif %}}
                {% else %}
                ,{ "id": "{{ topic.id }}", "name": {% if topic.name != null %}{{topic.name|json_encode|raw()}}{% else %}""{% endif %}}
                {% endif %}
            {% endfor %}
            ],
     "authors":
            [
            {% for author in entity.authors %}
                {% if loop.first %}
                { "id": "{{ author.id }}", "name": {% if author.name != null %}{{author.name|json_encode|raw()}}{% else %}""{% endif %}}
                {% else %}
                ,{ "id": "{{ author.id }}", "name": {% if author.name != null %}{{author.name|json_encode|raw()}}{% else %}""{% endif %}}
                {% endif %}
            {% endfor %}
            ]
         
}
