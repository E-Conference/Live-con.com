{
     "id": "{{ entity.id }}",
     "title": "{{ entity.title }}",
     "abstract": "{{ entity.abstract }}",
     "publishDate": "{{ entity.publishDate }}" , 
     "url": "{{ entity.url }}" ,       
     "publisher": "{{ entity.publisher }}" , 
     "subject":
            [
            {% for keyword in entity.subject %}
                {% if loop.first %}
                { "id": "{{ keyword.id }}", "name": "{{ keyword.name }}"}
                {% else %}
                ,{ "id": "{{ keyword.id }}", "name": "{{ keyword.name }}"}
                {% endif %}
            {% endfor %}
            ],
     "authors":
            [
            {% for author in entity.authors %}
                {% if loop.first %}
                { "id": "{{ author.id }}", "name": "{{ author.name }}"}
                {% else %}
                ,{ "id": "{{ author.id }}", "name": "{{ author.name }}"}
                {% endif %}
            {% endfor %}
            ]
         
}
